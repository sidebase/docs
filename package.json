{
  "name": "docus-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "preview": "nuxi preview",
    "generate": "nuxi generate && rm -rf docs && mv .output/public docs && touch docs/.nojekyll"
  },
  "devDependencies": {
    "@nuxt-themes/docus": "0.3.1",
    "nuxt": "^3.0.0"
  }
}
